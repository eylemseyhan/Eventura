@* @model EntityLayer.Concrete.EventsTicket

@{
    ViewData["Title"] = "Bilet Etkinliği Güncelle";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <div class="card">
        <div class="card-header">
            <h3>Bilet Etkinliği Güncelle</h3>
        </div>
        <div class="card-body">
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger">
                    @TempData["ErrorMessage"]
                </div>
            }
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success">
                    @TempData["SuccessMessage"]
                </div>
            }

            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="EventsTicketId" value="@Model.EventsTicketId" />

                <div class="form-group">
                    <label for="EventId">Etkinlik</label>
                    <select id="EventId" name="EventId" class="form-control" required>
                        <option value="">Etkinlik seçin</option>
                        @foreach (var eventItem in ViewBag.Events)
                        {
                            <option value="@eventItem.EventId" @(eventItem.EventId == Model.EventId ? "selected" : "")>
                                @eventItem.Title
                            </option>
                        }
                    </select>
                </div>

                <div class="form-group">
                    <label for="TicketCapacity">Bilet Kapasitesi</label>
                    <input type="number" id="TicketCapacity" name="TicketCapacity" class="form-control" value="@Model.TicketCapacity" required />
                </div>

                <div class="form-group">
                    <label for="Price">Fiyat</label>
                    <input type="number" id="Price" name="Price" value="@Model.Price" class="form-control" required />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label for="Name">Bilet Adı</label>
                    <input type="text" id="Name" name="Name" value="@Model.Name" class="form-control" required />
                </div>

                <div class="form-group">
                    <label for="SoldCount">Satılan Bilet Sayısı</label>
                    <input type="number" id="SoldCount" name="SoldCount" class="form-control" value="@Model.SoldCount" required />
                </div>

                <button type="submit" class="btn btn-primary">Güncelle</button>
            </form>
        </div>
    </div>
</div>
 *@